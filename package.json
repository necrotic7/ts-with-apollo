{
  "name": "wei-apollo",
  "version": "1.0.0",
  "description": "npm init npm install apollo-server graphql npm install typescript ts-node --save-dev npx tsc --init npx ts-node src/server.ts",
  "main": "src/server.ts",
  "scripts": {
    "start": "nodemon src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-server": "^3.13.0",
    "graphql": "^16.10.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.10",
    "nodemon": "^3.1.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
